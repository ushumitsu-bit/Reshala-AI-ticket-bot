# 🤝 Как внести лепту в общее дело (Contributing)

Хочешь помочь развитию инструмента? Мы только за. Но у нас тут свои порядки, чтобы не было хаоса.

## 📋 Понятия (Правила)

1. **Без самодеятельности в Master/Main.**
   Все изменения льем через **Pull Request (PR)**. В ветку `dev` или `main` (смотря как настроено) напрямую не коммитим.

2. **Код должен быть чистым.**
   Пиши комментарии там, где логика сложная. Переменные называй понятно (`$user_limit`, а не `$a`).

3. **Проверь перед отправкой.**
   Запусти у себя, убедись, что сервак не падает и скрипт не крашится. Если ломаешь обратную совместимость — пиши об этом жирным шрифтом.

## 🛠 Процесс (Как сделать подгон)

1. **Форкни репо** (кнопка Fork справа сверху).
2. **Сделай ветку** под свою задачу (`git checkout -b fix-sheiper-bug`).
3. **Накоди красоту**.
4. **Запушь в свой форк**.
5. **Создай Pull Request** к нам.

В описании PR четко распиши: чё сделал, зачем, и не сломал ли чего старого.

---
*Держим марку. Твой Решала.*
