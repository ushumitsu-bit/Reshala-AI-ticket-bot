# 🌍 Конфигурация Переменных Окружения

Приложение настраивается через файл `.env` в корневой директории.

## 📋 Обязательные Настройки

| Переменная | Описание | Пример |
|------------|----------|--------|
| `BOT_TOKEN` | **Обязательно.** Токен Telegram бота от @BotFather. | `1234567890:kbjkb...` |
| `REMNAWAVE_API_URL` | **Обязательно.** URL вашей панели Remnawave. | `https://panel.example.com` |
| `REMNAWAVE_API_TOKEN` | **Обязательно.** JWT токен администратора Remnawave. | `eyJhbG...` |
| `SUPPORT_GROUP_ID` | **Обязательно.** ID группы поддержки для топиков (начинается с `-100`). | `-1001234567890` |
| `ALLOWED_MANAGER_IDS` | **Обязательно.** Список ID менеджеров через запятую. | `123456789,987654321` |

## 🌐 Настройки Mini App и Web

| Переменная | Описание | Пример |
|------------|----------|--------|
| `SERVICE_NAME` | Название сервиса, отображаемое в ответах AI. | `SuperVPN` |
| `REACT_APP_BACKEND_URL`| **Обязательно.** Публичный URL API бэкенда. | `https://api.example.com` |
| `MINI_APP_DOMAIN` | **Обязательно.** Домен, где хостится Mini App. | `api.example.com` |
| `MINI_APP_URL` | Опционально. Прямой URL, если отличается от домена (для dev). | `http://localhost:3000` |
| `SKIP_AUTH` | **Безопасность.** Ставить `true` ТОЛЬКО для локальной разработки. Отключает проверку `initData`. | `false` |
| `PERSISTENCE_PATH` | Путь к файлу сохранения состояния бота (pickle). | `/data/bot_state.pickle` |

## 📦 База Данных

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `MONGO_URL` | Строка подключения MongoDB. <br>⚠️ **Docker:** Если не задано, используется `mongodb://mongodb:27017` (внутренняя сеть). Если задаете вручную для Docker — не используйте `localhost`! | `mongodb://mongodb:27017` |
| `DB_NAME` | Имя базы данных. | `reshala_support` |

## 💰 Bedolaga (Опционально)

Если вы используете биллинг Bedolaga.

| Переменная | Описание |
|------------|----------|
| `BEDOLAGA_API_URL` | URL вебхука/API Bedolaga. |
| `BEDOLAGA_API_TOKEN` | Токен API Bedolaga. |

## 🔐 Заметки по Деплою

- **Продакшн:** Убедитесь, что `SKIP_AUTH=false`.
- **Docker:** Переменные передаются в контейнеры через `docker-compose.yml`.
- **Frontend:** Переменная `REACT_APP_BACKEND_URL` "вшивается" в билд фронтенда. Если меняете URL API, нужно пересобрать контейнер фронтенда.
